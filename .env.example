# Environment Configuration
# Copy this file to .env and fill in the values

# Bug report intake endpoint.
# This fork has no default endpoint; if unset, bug-report submissions fail
# gracefully (the user is shown the "Copy Report" fallback).
# VITE_BUG_REPORT_ENDPOINT=http://localhost:3000/api/v1/bug-reports
